<div class="text-center user_title">
  <h1>ユーザー編集</h1>
</div>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    
    <%= form_for(@user) do |f| %>
      <%= render 'layouts/error_messages', model: f.object %>
      
      <div class="form-group">
        <div class="text-center">
          <div id="img_field" class="user_img_edit" onClick="$('#file').click()">
            <%# アイコン画像　正方形　角丸　画像部分をクリックするとファイル選択画面が出るようにしたい%>
            <% if @user.image.present? %>
              <%= image_tag @user.image.to_s, class: 'user_icon_md img-rounded', width: '130', height: '130' %>
            <% else %>
              <img class="user_icon img-rounded" src="/assets/f_f_object_115_s128_f_object_115_0bg.png">
            <% end %>
            <i class="fas fa-camera fa-3x"></i>
          </div>
        </div>
        <%= f.file_field :image, class: 'form-control', style: "display:none;", id: "file" %>
      </div>
      
      <div class="form-group">
        <%= f.label :name, 'ユーザー名(サイト上で表示されます)' %>
        <%= f.text_field :name, placeholder: '30文字以内', class: 'form-control' %>
      </div>
      
      <div class="form-group">
        <%= f.label :profile, 'プロフィール' %>
        <%= f.text_area :profile, placeholder: 'プロフィールを入力してください', class: 'form-control', rows: 5 %>
      </div>
      
      <%= link_to 'キャンセル', user_path(@user), class: 'btn btn-default btn-block' %><br>
      <%= f.submit '更新する', class: "btn btn-primary btn-block" %>
    <% end %>
  </div>
</div>