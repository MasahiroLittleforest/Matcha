<div class="text-center">
  <h1>プロジェクトをはじめる</h1>
</div>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    
    <%= form_for(@project) do |f| %>
      <%= render 'layouts/error_messages', model: f.object %>
      
      <div class="form-group">
        <%= f.label :title, 'タイトル' %>
        <%= f.text_field :title, placeholder: '40文字以内', class: 'form-control' %>
      </div>
      
      <div class="form-group">
        <%= f.label :content, '活動内容' %>
        <%= f.text_area :content, placeholder: '何をするのか、開催日、活動場所など、プロジェクトについての説明を書いてください', class: 'form-control', rows: 10 %>
      </div>
      
      <div class="form-group">
        <%= f.label :project_category_id, 'カテゴリー' %>
        <%= f.collection_select :project_category_id, ProjectCategory.all, :id, :name, { prompt: '選択してください'}, class: 'form-control' %>
      </div>
      
      <div class="form-group">
        <%= f.label :recruitment_numbers, '募集人数' %>
        <%= f.number_field :recruitment_numbers, min: 1, class: 'form-control' %>
        <%= f.check_box :all_or_nothing %> 募集人数に達しなければ開催しない
      </div>
      
      <div class="form-group">
        <%= f.label :deadline, '締切' %>
        <%= f.text_field :deadline, class: 'form-control datepicker' %>
      </div>
      
      <div class="form-group">
        <%= f.label :image, '画像' %>
        <%= f.file_field :image, class: 'form-control' %>
      </div>
      
      <%= f.submit '投稿する', class: "btn btn-primary btn-block" %>
    <% end %>
  </div>
</div>