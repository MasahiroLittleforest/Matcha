<% if current_user == @project.user %>
  <% unless @project.call_off || @project.deadline.past? %>
    <%= link_to 'このプロジェクトを中止する', call_off_project_path(@project), method: :put, data: { confirm: "一度中止すると募集を再開することはできません。本当に中止しますか？" }, class: 'btn btn-danger btn-block' %>
  <% end %>
<% end %>